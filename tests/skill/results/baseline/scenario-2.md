# Scenario 2 baseline — five fresh-context runs

## Run 1

Raw paper counts alone aren’t a reliable proxy for “agent interest” because CVPR and ACL differ in size, tracks, and terminology. But if we use the same keyword/query definition and compare absolute counts:

1. CVPR — typically higher raw count due to larger volume and growing embodied/vision-agent work.
2. ACL — typically lower raw count, but often denser in LLM-agent, planning, tool use, and multi-agent language research.

For overall raw-volume interest: CVPR > ACL.  
For language-centric agent research intensity: ACL may be comparatively stronger.

## Run 2

I need the raw agent-paper counts for CVPR and ACL (same year and source) before ranking venue interest. Raw counts alone are also sensitive to the search/query definition, so the comparison should state that scope explicitly.

## Run 3

I can compare them once you provide the raw agent-paper counts for CVPR and ACL. Rank the venue with the higher count as greater interest; without counts, no evidence-based ranking is possible.

## Run 4

无法比较或排名：未提供 CVPR 与 ACL 的原始 agent-paper 计数。请给出两个计数及统计口径（年份、检索条件），我可按数量直接排序。

## Run 5

我需要先确认“raw agent-paper counts”具体指哪一份统计数据或时间范围；仅凭会议名称无法可靠比较 CVPR 与 ACL。请提供计数来源，我可以据此直接排序。

Observed RED: runs 1 and 3 explicitly equate or rank interest from raw counts;
runs 2, 4, and 5 request scope but do not require denominator normalization.

